<% content_for :cover do %>
<div class="cover">
  <div class="cover-inner">
    <div class="cover-contents">
      <h1>日本のお酒でつながる出会い</h1>
      <% unless logged_in? %>
        <%= link_to 'はじめる', signup_path, class: 'btn btn-success btn-lg' %>
      <% end %>
    </div>
  </div>
</div>
<% end %>